import React, { useEffect } from 'react';
import { View, Text } from 'react-native';

const Cards = ({ type }) => {
  // api call depending on type

  return (
    <Text>
      Cards placeholder Cards placeholderCards placeholderCards placeholderCards
      placeholderCards placeholderCards placeholderCards placeholderCards
      placeholderCards placeholderCards placeholderz
    </Text>
  );
};

export default Cards;
